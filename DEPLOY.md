# 🎮 五子棋（Gomoku）游戏 - 打包完成

## 📦 可直接使用的便携版

已生成便携版包：`Gomoku_Portable/`

### 使用方式

#### Windows 用户（推荐）
1. 进入 `Gomoku_Portable` 目录
2. **双击** `START_GAME.bat`
3. 首次运行会自动安装依赖（pygame、numba、numpy）
4. 游戏窗口将自动启动

#### Linux/macOS 用户
```bash
cd Gomoku_Portable
chmod +x start_game.sh
./start_game.sh
```

或直接运行：
```bash
cd Gomoku_Portable
python gomoku_launcher.py
```

### 系统需求
- **Python 3.5+** 已安装并在 PATH 中
- 网络连接（首次运行时自动安装依赖）
- 4 GB 内存（推荐）

### 游戏控制

| 按键 | 功能 |
|------|------|
| 左击 | 放置棋子 |
| **A** | 切换 AI（启/关） |
| **P** | 切换 AI 方（1=黑，2=白） |
| **+/-** | 调整 AI 搜索深度 |
| **U/R** | 撤销/重做 |
| **N** | 新游戏 |
| **S** | 保存游戏 |
| **L** | 加载游戏 |
| **ESC** | 退出 |

---

## 📂 包内文件说明

```
Gomoku_Portable/
├── START_GAME.bat           # Windows 启动脚本（推荐双击）
├── start_game.sh            # Linux/macOS 启动脚本
├── gomoku_launcher.py       # Python 启动器
├── gomoku/                  # 游戏源代码
│   ├── main.py
│   ├── game.py              # 棋盘规则
│   ├── ai.py                # AI 引擎（minimax+α-β）
│   ├── ai_numba.py          # Numba JIT 加速
│   ├── ai_cy.pyx            # Cython 脚手架（可选）
│   ├── ui.py                # Pygame 用户界面
│   ├── storage.py           # 存档功能
│   └── __init__.py
├── requirements.txt         # Python 依赖清单
├── README.md                # 项目说明
└── PORTABLE_README.md       # 便携版说明
```

---

## 🚀 性能

- **AI 速度**：~1.15 秒/步（depth=2，Numba JIT 已启用）
- **优化**：
  - Numba JIT 编译（热循环加速）
  - Byte-grid 缓存键（快速序列化）
  - Minimax + α-β 剪枝 + killer moves + history heuristic
  - 评估缓存 + 转置表

---

## ⚙️ 进阶使用

### 修改 AI 难度

在 `gomoku/ui.py` 中修改默认深度（第 ~50 行）：
```python
self.ai_depth = 2  # 改为 3-4 获得更强 AI
```

### 查看游戏日志

游戏会在启动目录生成 `savegame.json` 用于存档。

---

## ❌ 故障排查

### "python: command not found"
- Python 3.5+ 未安装或未加入 PATH
- 解决：[下载 Python](https://www.python.org/downloads/) 并在安装时勾选"Add Python to PATH"

### 游戏卡顿
- 若使用深度 > 4，AI 搜索会变慢
- 解决：按 `-` 键降低搜索深度

### 缺少 pygame/numba
- 确保网络正常
- 手动运行：`python -m pip install pygame numba numpy`

---

## 🔧 完整编译版本（Linux/WSL）

若要编译 Cython 版本以获得更快的 AI：
```bash
cd Gomoku_Portable
pip install cython
python ../setup_cython.py build_ext --inplace
# 重启游戏自动使用编译的 .so/.pyd
```

---

**祝你游戏愉快！** 🎯
